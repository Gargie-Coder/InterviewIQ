# InterviewIQ Environment Configuration
# Optional: Provide an API key for live AI evaluation in mock interviews and resume parsing.
# If not provided, InterviewIQ uses the built-in intelligent heuristic engine and demo mode automatically.

# Google Gemini API Key (Optional)
GEMINI_API_KEY=

# OpenAI API Key (Optional alternative)
OPENAI_API_KEY=

# App Port (default 3000)
PORT=3000
